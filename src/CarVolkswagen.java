public class CarVolkswagen extends Car {

    CarVolkswagen(Color color, int year, WheelSize wheelSize, EngineVolume engineVolume) {
        super(color, Model.VOLKSWAGEN, year, wheelSize, engineVolume);
    }
}
